# Build output and generated types (also in .gitignore, which Prettier 3
# respects by default — kept explicit so formatting never depends on it)
dist/
.astro/

pnpm-lock.yaml
public/

# Content entries: Markdown with custom directives (::::tabs) and KaTeX math
# that Prettier's markdown printer can mangle — authors' prose stays untouched
src/content/
